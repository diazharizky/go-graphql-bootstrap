server:
  host: 0.0.0.0
  port: 3000

db:
  type: postgres
  postgres:
    host: '0.0.0.0'
    port: 5432
    user: postgres
    password: postgres
    dbname: postgres

mongodb:
  host: localhost
  port: 27017
  db: gqlbootstrap
